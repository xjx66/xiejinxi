# Checklist

- [x] 每个 `.carousel-item` 在脚部位置渲染椭圆形软阴影（径向渐变伪元素）
- [x] `.carousel-item.active` 的阴影比未激活状态更浓更大，并有过渡动画
- [x] 白天模式阴影颜色为 `rgba(0,0,0,0.25)` 左右；夜间模式为 `rgba(0,0,0,0.5)` 左右
- [x] 5 个角色的脚部在屏幕上处于同一水平基线（容差 < 5px）
- [x] 切换主题时阴影颜色平滑过渡（不闪跳）
- [x] 没有破坏原有交互：轮播切换、AI 对话、自动轮播守卫、模型加载、灯阵列、瓷砖阵列均正常

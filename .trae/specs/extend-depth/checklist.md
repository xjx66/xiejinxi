*- [x] `bgScene.fog.far` 更新为 700
- [x] `bgCamera.lookAt` 初始设置和滚动更新时的 Z 坐标均为 -450
- [x] `wall.position.z` 更新为 -445
- [x] `wallPanels` 实例的 Z 坐标生成更新为 -445 + wallPanelThickness / 2
- [x] `BG_Z_MIN` 常量更新为 -420
- [x] 确保画作 `paintingGroup.position.set` 仍然保持在原来的 `-294.6 + wallPanelThickness` 处未被修改

